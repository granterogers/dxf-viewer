<Application x:Class="DxfViewer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DxfViewer">
  <Application.Resources>
    <ResourceDictionary>
      <local:BoolToVisibilityConverter x:Key="BoolToVisibility"/>

      <!-- ===== Design tokens =====
           Every color, spacing step and text size in the app resolves here. Previously
           these hex values were repeated across three XAML files and two C# render paths,
           so a change had to be made in five places to stay consistent. -->
      <Color x:Key="CanvasBgColor">#12121E</Color>
      <SolidColorBrush x:Key="CanvasBg"     Color="#12121E"/>
      <SolidColorBrush x:Key="PanelBg"      Color="#1E1E30"/>
      <SolidColorBrush x:Key="ToolbarBg"    Color="#252535"/>
      <SolidColorBrush x:Key="WindowBg"     Color="#1E1E1E"/>
      <SolidColorBrush x:Key="TabStripBg"   Color="#1A1A2E"/>
      <SolidColorBrush x:Key="TabBg"        Color="#252540"/>
      <SolidColorBrush x:Key="StrokeSubtle" Color="#303048"/>
      <SolidColorBrush x:Key="StrokeStrong" Color="#404055"/>
      <SolidColorBrush x:Key="TextPrimary"  Color="#D0D0E0"/>
      <SolidColorBrush x:Key="TextMuted"    Color="#9090A0"/>
      <SolidColorBrush x:Key="TextFaint"    Color="#606070"/>
      <SolidColorBrush x:Key="AccentOrange" Color="#E07820"/>
      <SolidColorBrush x:Key="AccentOrangeDim" Color="#A85510"/>
      <SolidColorBrush x:Key="AccentBlue"   Color="#1565C0"/>
      <SolidColorBrush x:Key="AccentBlueDim" Color="#0D47A1"/>
      <SolidColorBrush x:Key="ErrorFg"      Color="#CC4444"/>
      <SolidColorBrush x:Key="ErrorFgDim"   Color="#AA6666"/>

      <!-- 4px spacing scale -->
      <Thickness x:Key="Space1">4</Thickness>
      <Thickness x:Key="Space2">8</Thickness>
      <Thickness x:Key="Space3">12</Thickness>
      <Thickness x:Key="GutterH">8,4</Thickness>

      <!-- Type scale: label / body / heading -->
      <system:Double xmlns:system="clr-namespace:System;assembly=mscorlib" x:Key="FontLabel">11</system:Double>
      <system:Double xmlns:system="clr-namespace:System;assembly=mscorlib" x:Key="FontBody">12</system:Double>
      <system:Double xmlns:system="clr-namespace:System;assembly=mscorlib" x:Key="FontHeading">16</system:Double>

      <Style x:Key="LabelText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="{StaticResource FontLabel}"/>
        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
      </Style>
      <Style x:Key="BodyText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="{StaticResource FontBody}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
      </Style>
      <Style x:Key="HeadingText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="{StaticResource FontHeading}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
      </Style>


      <!-- Small button used inside side panels -->
      <Style x:Key="PanelButton" TargetType="Button">
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Background" Value="{StaticResource TabBg}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StrokeStrong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="B" Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="B" Property="Background" Value="{StaticResource StrokeStrong}"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Shared blue button template — no hover/press color changes -->
      <ControlTemplate x:Key="BlueButtonTemplate" TargetType="Button">
        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}">
          <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
      </ControlTemplate>

      <!-- Standard toolbar button (blue, static color) -->
      <Style x:Key="ToolbarButton" TargetType="Button">
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="Background" Value="#1565C0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#0D47A1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template" Value="{StaticResource BlueButtonTemplate}"/>
      </Style>

      <!-- Toolbar toggle button (blue, static color) -->
      <Style x:Key="ToolbarToggleButton" TargetType="ToggleButton">
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="Background" Value="#1565C0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#0D47A1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ToggleButton">
              <Border x:Name="Chrome"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="Chrome" Property="Background" Value="#1976D2"/>
                </Trigger>
                <!-- Checked state has to repaint the template's own Border: the style's
                     Background setter is what TemplateBinding reads, so a plain Setter on
                     the ToggleButton is overridden by the hover trigger above. -->
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="Chrome" Property="Background"  Value="#E07820"/>
                  <Setter TargetName="Chrome" Property="BorderBrush" Value="#A85510"/>
                </Trigger>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition Property="IsChecked"   Value="True"/>
                    <Condition Property="IsMouseOver" Value="True"/>
                  </MultiTrigger.Conditions>
                  <Setter TargetName="Chrome" Property="Background" Value="#F08A2E"/>
                </MultiTrigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Close × button shown on tab hover -->
      <Style x:Key="TabCloseButton" TargetType="Button">
        <Setter Property="Width" Value="16"/>
        <Setter Property="Height" Value="16"/>
        <Setter Property="Margin" Value="6,0,0,0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="#606070"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border Background="{TemplateBinding Background}"
                      Width="{TemplateBinding Width}"
                      Height="{TemplateBinding Height}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter Property="Foreground" Value="#FF6B6B"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- TabControl: strip on top, content area below -->
      <Style TargetType="TabControl">
        <Setter Property="Background" Value="#12121E"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="TabControl">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="34"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- Tab strip -->
                <Border Grid.Row="0" Background="#1A1A2E">
                  <TabPanel IsItemsHost="True" HorizontalAlignment="Left" VerticalAlignment="Stretch"/>
                </Border>
                <!-- Content area -->
                <Border Grid.Row="1" Background="#12121E">
                  <ContentPresenter ContentSource="SelectedContent"/>
                </Border>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- TabItem: filename tab with active/hover states -->
      <Style TargetType="TabItem">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="TabItem">
              <Border x:Name="TabBorder"
                      Background="#252540"
                      BorderThickness="0"
                      Padding="12,0,8,0"
                      Height="34">
                <ContentPresenter ContentSource="Header"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsSelected" Value="True">
                  <Setter TargetName="TabBorder" Property="Background" Value="#2D2D50"/>
                  <Setter TargetName="TabBorder" Property="BorderThickness" Value="0,0,0,3"/>
                  <Setter TargetName="TabBorder" Property="BorderBrush" Value="#4FA8E8"/>
                </Trigger>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsSelected" Value="False"/>
                  </MultiTrigger.Conditions>
                  <Setter TargetName="TabBorder" Property="Background" Value="#2A2A48"/>
                </MultiTrigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

    </ResourceDictionary>
  </Application.Resources>
</Application>
